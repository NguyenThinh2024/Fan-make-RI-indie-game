# 🏗️ Strategy Pattern Architecture Diagram

## 1. Class Diagram

```
┌─────────────────────────────────────────────────────────────┐
│                    IEffectStrategy                          │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ + GetEffectName(): string                             │  │
│  │ + CanExecute(target: GameObject): bool                │  │
│  │ + Execute(target: GameObject, context: EffectContext) │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
            ▲                    ▲
            │ implements         │ implements
            │                    │
    ┌───────────────┐    ┌──────────────────┐
    │ DamageEffect  │    │ ShieldEffect     │
    │ Strategy      │    │ Strategy         │
    └───────────────┘    └──────────────────┘
            ▲                    ▲
            │                    │
    ┌───────────────┐    ┌──────────────────┐
    │ BuffEffect    │    │ ScoutEffect      │
    │ Strategy      │    │ Strategy         │
    └───────────────┘    └──────────────────┘
            ▲
            │
    ┌───────────────────┐
    │ StorageEffect     │
    │ Strategy          │
    └───────────────────┘


┌──────────────────────────────┐
│  EffectStrategyFactory       │
│  (Static Methods)            │
├──────────────────────────────┤
│ + CreateStrategy(type: str)  │
│   → IEffectStrategy          │
│ + CreateAllStrategies()      │
│   → IEffectStrategy[]        │
└──────────────────────────────┘


┌────────────────────────────────────────┐
│        EffectExecutor                  │
│     (MonoBehaviour)                    │
├────────────────────────────────────────┤
│ - strategyCache: Dictionary<str, I...> │
├────────────────────────────────────────┤
│ + ExecuteEffectFromGU(gu, target)      │
│ + ExecuteEffect(effect, target)        │
│ + ClearStrategyCache()                 │
└────────────────────────────────────────┘
         │ uses
         ▼
┌──────────────────────────────────────────────────┐
│        EffectContext                             │
├──────────────────────────────────────────────────┤
│ + value: float                                   │
│ + scalingRatio: float                            │
│ + cooldownSec: float                             │
│ + type: string                                   │
│ + areaType: string                               │
│ + slots: int                                     │
│ + deny: string[]                                 │
│ + hp: float                                      │
│ + absorbRate: float                              │
│ ... và nhiều fields khác                         │
└──────────────────────────────────────────────────┘


┌─────────────────────────────────────────────────────────────┐
│               Receiver Interfaces                           │
├─────────────────────────────────────────────────────────────┤
│ IDamageReceiver        IShieldReceiver      IBuffReceiver   │
│ ├─ TakeDamage()        ├─ AddShield()       ├─ AddBuff()    │
│                                                              │
│ IScoutReceiver         IStorageReceiver                      │
│ ├─ Scout()             ├─ AddStorage()                       │
└─────────────────────────────────────────────────────────────┘
```

## 2. Sequence Diagram - Thực Thi Effect

```
User Code          EffectExecutor      Factory         Strategy         Target
    │                  │                  │                │                │
    │ ExecuteEffect()  │                  │                │                │
    ├─────────────────→│                  │                │                │
    │                  │ CreateStrategy() │                │                │
    │                  ├─────────────────→│                │                │
    │                  │   DamageStrategy │                │                │
    │                  │←─────────────────┤                │                │
    │                  │                  │ CanExecute()   │                │
    │                  │                  ├───────────────→│                │
    │                  │                  │      true      │                │
    │                  │                  │←───────────────┤                │
    │                  │                  │ Execute()      │                │
    │                  │                  ├───────────────→│                │
    │                  │                  │                │ TakeDamage()   │
    │                  │                  │                ├───────────────→│
    │                  │                  │                │    updated     │
    │                  │                  │                │←───────────────┤
    │                  │                  │    done        │                │
    │                  │                  │←───────────────┤                │
    │                  │      done        │                │                │
    │                  │←─────────────────┤                │                │
    │      done        │                  │                │                │
    │←─────────────────┤                  │                │                │
```

## 3. Component Flow Diagram

```
┌─────────────────────────┐
│    GuData              │
│  (Cached by GUFactory)  │
└────────────┬────────────┘
             │
             ▼
┌─────────────────────────────────────┐
│   EffectExecutor                    │
│   ExecuteEffectFromGU()             │
└────────────┬────────────────────────┘
             │
             ├─→ effect.damage?
             │   ├─→ CreateStrategy("damage")
             │   ├─→ DamageEffectStrategy
             │   ├─→ CanExecute(target)
             │   └─→ Execute(target, context)
             │       └─→ target.TakeDamage()
             │
             ├─→ effect.shield?
             │   ├─→ CreateStrategy("shield")
             │   ├─→ ShieldEffectStrategy
             │   ├─→ CanExecute(target)
             │   └─→ Execute(target, context)
             │       └─→ target.AddShield()
             │
             ├─→ effect.buff?
             │   ├─→ CreateStrategy("buff")
             │   ├─→ BuffEffectStrategy
             │   ├─→ CanExecute(target)
             │   └─→ Execute(target, context)
             │       └─→ target.AddBuff()
             │
             ├─→ effect.scout?
             │   ├─→ CreateStrategy("scout")
             │   ├─→ ScoutEffectStrategy
             │   ├─→ CanExecute(target)
             │   └─→ Execute(target, context)
             │       └─→ target.Scout()
             │
             └─→ effect.storage?
                 ├─→ CreateStrategy("storage")
                 ├─→ StorageEffectStrategy
                 ├─→ CanExecute(target)
                 └─→ Execute(target, context)
                     └─→ target.AddStorage()
```

## 4. Object Interaction Diagram

```
┌──────────────┐         ┌───────────────────┐         ┌──────────────┐
│   GUFactory  │         │  EffectExecutor   │         │    Target    │
│              │         │                   │         │   GameObject │
└──────────────┘         └───────────────────┘         └──────────────┘
     │                           │                             │
     │ Get("GU001")              │                             │
     ├──────────────────────┐    │                             │
     │ GuData gu            │    │                             │
     └──────────────────────┤    │                             │
                            │    │                             │
                            │ ExecuteEffectFromGU(gu, target)  │
                            ├─────────────────────────────────→│
                            │                                  │
                            │ ExecuteEffect(effect, target)    │
                            ├──────────────┐                   │
                            │ CreateStrategy("damage")         │
                            │ CanExecute()                      │
                            │ Execute()                         │
                            ├─────────────────────────────────→│
                            │ TakeDamage(50)                    │
                            │                                  │
                            │←─────────────────────────────────┤
                            │ (health -= 50)                   │
```

## 5. Inheritance Hierarchy

```
MonoBehaviour
    │
    ├─→ EffectExecutor
    │   └─→ Uses: IEffectStrategy instances
    │       ├─→ DamageEffectStrategy
    │       ├─→ ShieldEffectStrategy
    │       ├─→ BuffEffectStrategy
    │       ├─→ ScoutEffectStrategy
    │       └─→ StorageEffectStrategy
    │
    ├─→ SimpleHealth (implements IDamageReceiver)
    ├─→ SimpleShield (implements IShieldReceiver)
    ├─→ SimpleBuff (implements IBuffReceiver)
    ├─→ SimpleScout (implements IScoutReceiver)
    └─→ SimpleStorage (implements IStorageReceiver)
```

## 6. Data Flow Diagram

```
                         ┌─────────────────┐
                         │   GuData.cs     │
                         │  (ScriptableObj)│
                         └────────┬────────┘
                                  │
                    ┌─────────────┼─────────────┐
                    │             │             │
                    ▼             ▼             ▼
            ┌──────────────┐ ┌─────────┐ ┌──────────┐
            │ effect: Effect│ │ stats   │ │ ...      │
            └────────┬─────┘ └─────────┘ └──────────┘
                     │
        ┌────────────┼────────────┬────────────┬──────────────┐
        │            │            │            │              │
        ▼            ▼            ▼            ▼              ▼
   ┌────────┐  ┌────────┐  ┌────────┐  ┌─────────┐  ┌──────────────┐
   │ damage │  │ shield │  │ buff   │  │ scout   │  │ storage      │
   └────┬───┘  └────┬───┘  └────┬───┘  └────┬────┘  └──────┬───────┘
        │           │            │            │             │
        ▼           ▼            ▼            ▼             ▼
   ┌────────────┐ ┌─────────┐ ┌──────────┐ ┌──────────┐ ┌────────┐
   │ Strategy   │ │Strategy │ │Strategy  │ │Strategy  │ │Strategy│
   │ (Damage)   │ │(Shield) │ │(Buff)    │ │(Scout)   │ │(Storage)
   └────────┬───┘ └────┬────┘ └────┬─────┘ └────┬─────┘ └───┬────┘
            │          │            │            │           │
            └──────────┼────────────┼────────────┼───────────┘
                       │
                       ▼
            ┌──────────────────────┐
            │  Target (GameObject) │
            └──────┬───────────────┘
                   │
        ┌──────────┼──────────┬──────────┬──────────┐
        │          │          │          │          │
        ▼          ▼          ▼          ▼          ▼
   ┌────────┐ ┌───────┐ ┌────────┐ ┌──────┐ ┌─────────┐
   │IDamage │ │IShield│ │IBuff   │ │IScout│ │IStorage │
   │Receiver│ │Receiver│ │Receiver│ │Receiver│ │Receiver
   └────────┘ └───────┘ └────────┘ └──────┘ └─────────┘
```

## 7. Pattern Comparison

```
TRƯỚC (Structured):                  SAU (Strategy Pattern):

Effect                               IEffectStrategy
├─ DamageEffect                      ├─ DamageEffectStrategy
├─ ShieldEffect                      ├─ ShieldEffectStrategy
├─ BuffEffect                        ├─ BuffEffectStrategy
├─ ScoutEffect                       ├─ ScoutEffectStrategy
└─ StorageEffect                     └─ StorageEffectStrategy
                                     
Thực thi:                           Thực thi:
if (effect.damage != null)           IEffectStrategy s =
  ExecuteDamage();                     FactoryCreate();
if (effect.shield != null)           s.Execute(target);
  ExecuteShield();
... (10 lines of if-else)

(Khó maintain)                      (Dễ maintain & mở rộng)
```

---

**Lưu ý**: Diagrams này là minh họa. Code thực tế có thể có thêm details.
